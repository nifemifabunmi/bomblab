//
//  main.c
//  practices
//
//  Created by nifemi22@belahs.org on 2/6/25.
//

#include <stdio.h>
//a Write a program that acts as a calculator.
//Gets input from the user in the format: "%c %f %f" (char and two floats)
//Calculates the result from the operation (the char) and the two floats
//Prints the result from 2.
//Possible cases for the char (the operation) will be +, -, *, /

/*int main(void) {
    // Write code here
    float f1;
    float f2;
    char c1;
    printf("Give to float numbers with a character +, -, * or / in front of it:");
    scanf("%c\n%f\n%f", &c1, &f1, &f2);
    
    float ans;
    if ( c1 == '+'){
        ans = f1 + f2;
    }else if ( c1 == '-') {
        ans = f1 - f2;
    }else if ( c1 == '*') {
        ans = f1 * f2;
    } else if ( c1 == '/') {
            ans = f1 / f2;
        }else {
            return 0;
    }
        printf("%f", ans);
    return 0;
};
*/
/*int main (void) {
    int i = 1;
    do{
        printf("hello\n");
        i++;
    }
    while (i <= 10);
    //{
        //printf("hello\n");
        //i++;
    
    return 0;
};
 
 int main(void) {
     for (int i = 0; i < 100; i += 3) {
         // Don't change above this line
         if (i > 21)
             break;
             // Don't change below this line
             printf("%d\n", i);
         }
         return 0;
 };
 int main(void) {
     // Write code here
     int num;
     printf("Write an int:");
     scanf("%d", &num);

     for (int i = 1; i > num; i++) {
         if (!(i % 3 == 0 || i % 7 == 0 )) continue;
             printf("%d\n", i);
         }
     return 0;
 };
*/
